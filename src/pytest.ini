[pytest]
# Put the Lambda source directory on sys.path so tests can import the handler
# module directly, instead of each test file patching sys.path itself. tests is
# on the path too, so shared test constants can be imported from conftest.
pythonpath = . tests
testpaths = tests
